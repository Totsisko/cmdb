import React from 'react';
import MenuButton from './MenuButton'; // Import the MenuButton component

const AltarServerDashboard = () => {
  return (
    <div>
      <MenuButton /> {/* Add MenuButton at the top */}
      <h1>Altar Server Dashboard</h1>
      {/* Add more content for Altar Server here */}
    </div>
  );
};

export default AltarServerDashboard;
